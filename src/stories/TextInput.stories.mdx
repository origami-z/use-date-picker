import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { TextInput } from "../TextInput";

<Meta title="MDX/TextInput" component={TextInput} />

# TextInput

With `MDX` we can define a story for `TextInput` right in the middle of our
Markdown documentation.

<!--- This is your Story template function, shown here in React -->

export const Template = (args) => <TextInput {...args} />;

<Canvas>
  <Story
    name="Unchecked"
    args={{
      label: "Unchecked",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Checked"
    args={{
      label: "Checked",
      checked: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary"
    args={{
      label: "Secondary",
      checked: true,
      appearance: "secondary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
